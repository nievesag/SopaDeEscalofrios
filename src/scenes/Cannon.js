class Cannon extends Phaser.GameObjects.Sprite {
    constructor(scene, x, y) {
      super(scene, x, y, 'cannon');

      
      let posX, posY;
      let dir;
      let force;
      let h, w;
    }
  }