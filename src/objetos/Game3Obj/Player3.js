class Player3 extends Phaser.GameObjects.Sprite {
    constructor(scene, x, y) {
      super(scene, x, y, 'player3');

      this.scene.add
      let posX, posY;
      let dir;
      let force;
      let h, w;



    }
  }